---
steps:
  - script: 'docker run --name coworker-container -e POSTGRES_PASSWORD=supasekrit -d postgres'
  - script: './.ci/wait-for-it.sh 5432'
    timeoutInMinutes: 1
  - script: '(cd e2e/coworker-e2e/ && java -jar ./target/CoworkerE2e.jar)'
    timeoutInMinutes: 2
  - script: 'docker stop $(docker ps -aq)'
  - script: 'docker rm $(docker ps -aq)'
  - script: 'docker rmi $(docker images -q)'
