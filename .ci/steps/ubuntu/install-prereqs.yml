---
steps:
  - script: 'sudo apt-get update'
  - script: 'sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common'
  - script: 'curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -'
  - script: 'sudo add-apt-repository -y "deb [arch=amd64] https://download.docker.com/linux/ubuntu/xenial/stable"'
  - script: 'sudo apt-get update'
  - script: 'sudo apt-get install -y docker-ce'
